---
title: shadowModelViewInverse
description: shadowModelViewInverse
sidebar:
  label: shadowModelViewInverse
  order: 1
---

### `uniform mat4 shadowModelViewInverse;`

Equal to [`inverse(shadowModelView)`](/reference/uniforms/matrices/shadowmodelview), but calculated on the CPU to avoid running the expensive `inverse()` function on the GPU.