---
title: gbufferModelViewInverse
description: gbufferModelViewInverse
sidebar:
  label: gbufferModelViewInverse
  order: 1
---

### `uniform mat4 gbufferModelViewInverse;`

Equal to [`inverse(gbufferModelView)`](/reference/uniforms/matrices/gbuffermodelview), but calculated on the CPU to avoid running the expensive `inverse()` function on the GPU.