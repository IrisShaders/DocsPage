---
title: projectionMatrixInverse
description: Transforms coordinates from clip space to view/eye space.
sidebar:
  label: projectionMatrixInverse
  order: 1
  badge:
    text: 1.17+
    variant: danger
---
import { Aside } from '@astrojs/starlight/components';

<Aside type="danger">This uniform only works with the "core profile" in Minecraft `1.17` and newer. It is recommended to use the "compatibility profile" with Iris for better support. </Aside>

### `uniform mat4 projectionMatrixInverse;`

Equal to [`inverse(projectionMatrix)`](/reference/uniforms/matrices/projectionmatrix), but calculated on the CPU to avoid running the expensive `inverse()` function on the GPU.