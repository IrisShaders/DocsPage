---
title: gbufferProjectionInverse
description: gbufferProjectionInverse
sidebar:
  label: gbufferProjectionInverse
  order: 1
---

### `uniform mat4 gbufferProjectionInverse;`

Equal to [`inverse(gbufferProjection)`](/reference/uniforms/matrices/gbufferprojection), but calculated on the CPU to avoid running the expensive `inverse()` function on the GPU.